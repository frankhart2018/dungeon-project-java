package node;

/*
  Since the user does not need to create instances of this enum, it is made package-private,
  and all the user-facing methods return a string representation of these values.
 */
enum Treasure {
  SAPPHIRE,
  DIAMOND,
  RUBY,
}
